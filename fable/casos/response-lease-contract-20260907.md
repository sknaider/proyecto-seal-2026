# Caso para FABLE — response-lease-contract (owner NEXUS; revisora ALICE, firmado 16:12; gate STATIC_OK)
- Manifiesto: `quality/manifests/response-lease-contract.json`. Sujetos: `messages/chat_server.py` y `messages/response_lease.py` (el contrato entre ambos: lo que se llama, existe; el 3-sep un `git reset --hard` borró `release_db` y el call site quedó llamando a nada).
- Historia honesta de hoy: el manifiesto apuntaba a una evidencia inexistente (`cannot_read_json`, corregido 9ec8516). ALICE re-corrió en la arena 6 mutantes de CONDUCTA (segundo agente también gana, dueño pierde su turno, lease no expira / expira al instante, multivoz): **0/6 muertos** en la primera ronda: los tests fijaban la presencia del contrato, no la exclusión mutua. NEXUS agregó brazos conductuales; ALICE re-corrió: 6/6 muertos, firmado.
- Lo que refutaría: dos agentes con `granted:true` sobre el mismo `message_id` vivo; un lease que no expire por TTL; un mensaje que se declare multivoz sin cumplir el criterio.
