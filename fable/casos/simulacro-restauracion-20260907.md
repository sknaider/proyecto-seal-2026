# Caso para FABLE — simulacro-restauracion-20260907 (owner JARVIS; revisor NEXUS, firmado 14:59; gate STATIC_OK)
- Manifiesto: `quality/manifests/simulacro-restauracion-20260907.json`. Sujeto: `tools/seal_restaurar_desde_nfs.sh`. Tests: `tools/tests/test_seal_restaurar_desde_nfs_v1.py` (9 brazos: unit, 4 negativos con señuelo, 2 controles, positivo real, control de pisos).
- Qué: carril 6 de la spec resiliente. Reconstruye la casa desde la foto del NFS (repo, unidades, memoria de Claude, config sin secretos) en un directorio vacío bajo `/tmp/seal-restauracion-*` (guarda `# GUARDA-DESTRUCTIVA`, resuelta con realpath) y restaura el dump completo en un Postgres desechable; imprime métricas y falla con motivos explícitos.
- Criterio §8 de la spec: la casa vuelve en < 1 h sin rescatar nada de la memoria de un proceso. Medido hoy tres veces (13:11, 15:08): **42 s**, 19.261 archivos, 165 unidades + 87 timers, 264 tablas, 182.155 memorias, 12 esquemas (14 tablas de `orion_exam`), 0 secretos, 0 críticos faltantes.
- Historia honesta: las tres primeras corridas fallaron (rsync sin padres; arranque doble de la imagen Postgres; extensión `vector` fuera de `soul_v3`); cada causa dejó un fix. NEXUS refutó el veredicto (0 unidades y `~/.config` ausente daban OK): ahora UNIDADES>=100 y TIMERS>=50 entran al veredicto y tres rutas bajo `.config` son críticas; tests negativo y control con foto señuelo.
- Lo que refutaría: una foto sin `systemd_user` que dé OK; un destino con forma de home bajo /tmp que no sea rechazado o que pierda su testigo; un contenedor `seal-restauracion-*` que sobreviva al script; una foto con dump sólo de `soul_v3` que no reporte 0 tablas `orion_exam`.
- Lo que NO cubre: reinicio de unidades desde la copia (carril 4/5), secretos (por diseño no están en la foto), consistencia transaccional de la copia caliente de Neo4j (etiquetada como no acreditada).
