{
  "name": "seal-desktop",
  "version": "0.1.0",
  "description": "SEAL Studio — Desktop Agent (Tauri v2 wrapper)",
  "private": true,
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri",
    "build:linux": "./build-linux.sh",
    "build:windows": "powershell -ExecutionPolicy Bypass -File ./build-windows.ps1 -Arch x64"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
