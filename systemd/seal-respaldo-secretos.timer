[Unit]
Description=SEAL — copia cifrada del estado esencial, diaria

[Timer]
OnCalendar=*-*-* 03:30:00
# Persistent para que un host apagado a las 03:30 no se saltee el dia entero.
Persistent=true
RandomizedDelaySec=10min

[Install]
WantedBy=timers.target
