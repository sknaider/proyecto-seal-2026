[Service]
# Dedicated cross-agent maintenance login. File is local, owner-only (0600);
# the repository stores only this reference, never the DSN.
EnvironmentFile=%h/.config/seal/consolidate_db_runtime.env
