[Service]
# ADA only: compile authenticated A2 engineering missions. The deterministic
# daemon never launches a model; seal-ada-nerves-worker owns the isolated
# tool-less local Ollama runtime and byte-attested receipt.
Environment=SEAL_NERVES_AGENT_HANDOFF=1
