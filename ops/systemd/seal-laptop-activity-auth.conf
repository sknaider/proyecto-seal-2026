[Service]
# Raw token is never stored in the unit or process argv. The API validates
# owner/mode/non-symlink before binding and fails closed if this file is absent.
Environment=LAPTOP_API_TOKEN_FILE=%h/.config/seal/postgres-services/seal-laptop-activity.token
