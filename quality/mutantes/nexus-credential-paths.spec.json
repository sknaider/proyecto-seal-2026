{
 "change_id": "nexus-credential-paths",
 "reviewer": "ALICE",
 "manifest": "quality/manifests/nexus-credential-paths.json",
 "tests": [["tests/test_nexus_credential_paths_v1.py"]],
 "mutants": [
  {
   "id": "el-checkpoint-vuelve-a-cablear-una-credencial",
   "subject": "messages/session_checkpoint.py",
   "ancla": "    DB_URL = os.environ.get(\"SEAL_DB_URL\", \"\")",
   "reemplazo": "    DB_URL = os.environ.get(\"SEAL_DB_URL\", \"postgresql://seal:seal2026soul@localhost:5433/seal_memory\")",
   "why": "vuelve el literal como ultimo recurso: exactamente la clase que FABLE hizo ELIMINAR, no reordenar"
  },
  {
   "id": "el-checkpoint-deja-de-fallar-cerrado",
   "subject": "messages/session_checkpoint.py",
   "ancla": "    if not DB_URL:\n        raise RuntimeError(",
   "reemplazo": "    if False:\n        raise RuntimeError(",
   "why": "sin credencial ya no grita: el checkpoint sigue con DSN vacio y el fallo aparece disfrazado de problema de la base"
  },
  {
   "id": "el-heartbeat-acepta-quedarse-sin-credencial",
   "subject": "memory/seal_heartbeat.py",
   "ancla": "_DB_URL = pg_dsn(required=True)",
   "reemplazo": "_DB_URL = pg_dsn(required=False)",
   "why": "el latido arranca con DSN vacio en vez de negarse: un heartbeat que miente es peor que uno que no arranca"
  },
  {
   "id": "config-deja-de-poblar-desde-seal-secrets",
   "subject": "memory/config.py",
   "ancla": "    _seal_pg_dsn()          # efecto: puebla os.environ con PG_* y SEAL_*",
   "reemplazo": "    pass                    # efecto: puebla os.environ con PG_* y SEAL_*",
   "why": "los settings se quedan con los defaults de DESARROLLO en vez de los secretos reales"
  }
 ]
}
