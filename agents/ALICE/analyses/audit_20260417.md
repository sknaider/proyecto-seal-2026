# Auditoría Estratégica — Team SEAL
> Autor: JARVIS  |  Fecha: 2026-04-17 14:07 -05  |  Snapshot 5 min

## ✅ Saludables (no tocar)

| Componente | Estado |
|---|---|
| PostgreSQL (seal-memory-db) | Up 23h, healthy, puerto 5433 |
| Neo4j (soul-neo4j) | Up 23h, responde RETURN 1 |
| Qdrant (soul-qdrant) | Up 23h, healthz PASS |
| Claudes tmux | 3/3 activos: seal-ada, seal-alice, seal-jarvis |
| ws_listeners | 1 por agente (ADA/ALICE/JARVIS) — tras limpiar orphan |
| chat_server | PID 230865, activo desde 16-abr |
| Disco / | 1.7T libre de 3.7T (53% uso) |
| systemd timers | 24 activos (heartbeats, watchers, nerves, checkpoints) — 0 tokens Claude |

## ⚠️ Flags estratégicos

### F1 — DUM heartbeat stale
- `dum_heartbeat.json` modificado por última vez: **2026-04-16 15:14** (hace ~23h).
- Resto de agentes actualiza heartbeat cada ~5 min.
- Posibles causas: DUM murió, o su timer `dum_heartbeat.py` quedó colgado, o fue migrado y olvidamos limpiar.
- **Recomendación:** ADA verificar si DUM está vivo. Si no, decidir: restart o deprecar el archivo.

### F2 — Heartbeats NO llegan a event_log DB
- Último heartbeat en tabla `event_log` de cualquier agente: **2026-04-13 16:02** (hace 4 días).
- Heartbeats sí se escriben a JSON files (prueba: `jarvis_claude_heartbeat.json` modificado hace 1 min).
- Brecha: script que escribe heartbeat al JSON no está escribiendo al DB. Divergencia DB vs filesystem.
- Impacto: `event_log_query(event_type='heartbeat')` da datos obsoletos → consultas de peer-health pueden leer falso positivo de "agente muerto".
- **Recomendación:** ADA/DUM revisar `dum_heartbeat.py` o el script equivalente, reconectar al INSERT en event_log.

### F3 — Orphan ws_listener JARVIS (detectado y pendiente de limpiar)
- Durante la sesión, Monitor tool falló varias veces; quedó PID 1456498 como ws_listener JARVIS antiguo.
- Nuevo ws_listener (1465924) arrancó con while-loop auto-restart.
- **Acción inmediata:** kill 1456498 para evitar doble consumo de eventos.

### F4 — Workaround rule_set → memory_store
- Bug del trigger `trg_audit_fn` (ver `/messages/bug_rule_set_diagnosis.md`) bloquea `rule_set`.
- Regla de cross-agent non-intervention quedó como memoria #4772, no como rule.
- Impacto: hook de active_recall no la trae como "REGLA ACTIVA", solo como memoria.
- **Dependencia:** fix del trigger antes de poder migrar a tabla rules.

### F5 — Saturación memoria JARVIS vs equipo
- JARVIS: 236 memorias. ADA: 332. ALICE: 45.
- ALICE es muy joven (creada hace pocas semanas). Su curva de crecimiento es la esperada.
- No hay problema ahora, pero vigilar saturación Qdrant pg_vector a medida que ADA cruce 500.

## 📊 Métricas snapshot

| Métrica | Valor |
|---|---|
| Rules activas | 31 (22 critical + 8 high + 1 normal) |
| Memorias totales | ~656 (332+236+45+otras) |
| Timers systemd | 24 |
| Conexiones WS activas (chat_server) | 8 al momento de capturar pre-audit |
| Queue chat_server | 13,093 msgs persistidos; cursores=1725/agente; 237 pendientes/agente (límite 500 memoria, no data loss — corrección de ALICE) |

## 🎯 Recomendaciones priorizadas

1. **ADA (fix):** aplicar patch de trigger (`/messages/bug_rule_set_diagnosis.md`) — desbloquea rule_set.
2. **ADA (fix):** revisar dum_heartbeat / heartbeats al event_log — F1 + F2.
3. **ADA (fix):** watchdog singleton para ws_listener (ya estaba en tu pila).
4. **JARVIS (migrar):** post-fix trigger → migrar memoria #4772 a tabla `rules` con `rule_set`.
5. **Equipo:** revisar buffer en memoria del chat_server (500 msgs). No hay data loss — mensajes persisten en `chat_messages` (13,093). Pero si backlog de lectura crece sostenido, considerar aumentar ventana o flush más agresivo.

---

> Estado general: **Sistema operativo y estable**. Bugs conocidos están identificados y asignados. Ningún servicio crítico caído. William tiene razón en quejarse del copy-paste (bug UX real), pero el backend del equipo SEAL responde como esperado.
