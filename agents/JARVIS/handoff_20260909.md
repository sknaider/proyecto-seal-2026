📋 **Handoff de JARVIS — 9-sep. Qué quedó en vuelo, quién lo tiene, y qué NO se puede cerrar solo.**

## Cerrado y en git

```console
7 expedientes re-verificados CORRIENDO la mutacion y firmados   commit beceda0, pusheado
3 tests mios reconstruidos desde sus manifiestos                 EN HEAD, verificado
4 expedientes de ADA revisados y firmados esta noche             PASSED los cuatro
```

## Esperando a otro (no avanza sin ellos)

```text
chat-idem-dedup-durable        NEXUS  sobreviviente REAL: se puede volver inalcanzable
                                      la dedup durable y los 7 tests siguen verdes
coordinator-cutover            ADA    4 tests rojos; el clasificador devuelve
                                      'cascade' donde el test espera 'discussion'
guardian-solo-ejecutable       NEXUS  2 comandos apuntan a pruebas renombradas (rc=4)
consolidate-dedup-bounded      NEXUS  coverage_regression 19.529<20.0 — merge_redundant
                                      esta al 100% de lineas Y ramas; el 19,5% mide el
                                      archivo entero. NO bajar el umbral yo: es amañarlo
jarvis-dm-poller-env-dsn       ADA    espera su firma
```

## Bloqueado por credencial, NO por trabajo

`runtime-instance-passthrough` vuelve a `bytes_stale` cada vez que se toca su script de entrega, y **yo no lo puedo cerrar: su brazo de efecto necesita `SEAL_PG_DSN` y no la tengo**. Quien la tenga lo cierra en dos minutos. No es deuda técnica, es una llave que me falta.

## Carriles abiertos, con dueño a definir

1. **332 de 499 tests fuera de todo manifiesto** — el gate ve un tercio. Primero clasificar cuáles tocan recursos vivos, después correr los inocuos. **No a ciegas.**
2. **Cuota de búsqueda compartida** — `_rate_check(tool_name)`, cola por herramienta y no por agente. 30 req/min para los cinco. Es disponibilidad, no corrupción (me corregí en eso).
3. **`revalidar <manifiesto>`** — el ciclo mecánico que hice a mano toda la noche. Condición de diseño: **que no pueda firmar cuando algo falla.**
4. **`_post_rule` contradice `CLAUDE.md`** — el monitor le dice a cada agente que use `curl`, que la regla prohíbe. Una línea en `seal_monitor_filter.py:113,157`.

## Lo que le debo a William

Su plan AGI sigue esperando su orden. El frente de investigación —el único que no toca a nadie— avanzó: taxonomía MAST y verificación determinista, los dos reportados con fuente primaria.

---

**Y lo que me llevo del día, sin adornarlo:** ADA me corrigió **ocho veces** y las ocho tuvo razón. Ninguna fue por falta de cuidado en lo difícil: **fueron por no verificar lo que me parecía obvio.** Firmé un expediente el 8-sep con un recibo que decía «SHAs verificados» y dejé pasar un sobreviviente. Hoy re-firmé todo corriendo la mutación, y por eso encontré lo que encontré.

---

## CORRECCIÓN al handoff (JARVIS, 10-sep 00:07) — el despliegue NO está verificado

Declaré cerrado el despliegue del arreglo de búsqueda. **Retirado.** ADA lo midió y tenía razón.

```text
ESTABLECIDO      el codigo esta en git          b6d1142, verificado en HEAD
                 el MCP reinicio                PID 1294921 (el anterior, 2 dias)
                 el adaptador arreglado anda    13 preguntas sin respuesta -> 0

NO ESTABLECIDO   que el MCP EN EJECUCION use ese codigo
```

El test de entrega dice en su línea 22: *«No pasa por MCP a propósito: llama al adaptador
en proceso»*. Importa `mcp_server_v4` dentro de pytest y llama a `adaptador.query_points()`
directo. **Los 14,96 s prueban que el adaptador funciona; no qué servicio atendió nada.**

**Tres instrumentos intenté y los tres fallan para esto:**
- `mtime` anterior al arranque — compatible con haber cargado, no lo prueba
- huella de objetos código — colisiona si se omite `co_names` (medido por ADA)
- este test — no pasa por el MCP

**Lo que cierra el despliegue:** una consulta real POR la interfaz del MCP, comparada contra
el comportamiento arreglado. Sin eso el estado es **desplegado sin verificar** — que no es
verificado, y tampoco es roto.

**Y el hueco de fondo, que es carril:** el chat tiene `/__version`; el MCP no tiene ninguno.
Por eso el despliegue del chat se comprueba y el del MCP hay que inferirlo.

---

## Estado al cierre (JARVIS, 10-sep 00:55)

### Cerrados y en PASSED
```text
chat-idem-dedup-durable          el sobreviviente que reporte YA MUERE (brazo de NEXUS
                                 sobre agents_send). 6/6 re-corridos por mi.
consolidate-live-schema          bridge-runtime-instance      dm-channel-over-to
+ 6 expedientes de ADA de la noche (rescate ANN, v5.1, v3.2, limpieza del bench,
  seal-bench-mide-SOUL, y la condicion MS-c de FABLE)
```

### El costo estructural, MEDIDO — no es teoria
```text
EXPOSICION       9 manifiestos DECLARAN messages/chat_server.py como sujeto
                   -> tocarlo PUEDE vencer nueve firmas    (numero de NEXUS, verificado)
COSTO REALIZADO  7 se vencieron con el cambio de hoy       (el mio)
                   las otras 2 no: una la re-firme yo despues del cambio, la otra ya
                   estaba vencida de antes
Para PLANIFICAR sirve el 9: es lo que cuesta tocar ese archivo.
Para saber que paso hoy, el 7.

7 firmas caidas de una sola vez  <-  un arreglo legitimo en messages/chat_server.py
    ada-sala-privada · alice-cutover-exclusion-mutua · channel-acl
    chat-redelivery-acusa · coordinator-cutover · private-flag-gates-reads
    runtime-instance-passthrough
22 firmas vencidas en el arbol en total
```
Seis de esas siete las firme yo horas antes, corriendo la mutacion entera cada vez.
**Nadie hizo nada mal:** NEXUS agrego el brazo que yo le reclame. La regla es correcta
—cada firma cubre el archivo entero— pero sin un `revalidar <manifiesto>` el costo hace
que la gente EVITE tocar los archivos con muchos manifiestos, que es peor que el costo.

**No las re-firme de madrugada a proposito:** son siete corridas de mutacion, y hacerlas
cansado es exactamente como se firma «SHAs verificados» sin correr nada — mi error del
8-sep, que hoy costo el dia entero encontrar.

### Sigue esperando a otro
```text
coordinator-cutover        ADA    1 rojo REAL preexistente: test_classifier lee la
                                  bandera de PRODUCCION (messages/.cascade_mode = ON),
                                  no es hermetico. Acordado: aislar en tmp_path y cubrir
                                  OFF->discussion y ON->cascade. NO tocar el assert.
guardian-solo-ejecutable   NEXUS  2 comandos apuntan a pruebas renombradas (rc=4)
consolidate-dedup-bounded  NEXUS  coverage_regression: merge_redundant al 100% de lineas
                                  Y ramas; el 19,5% mide el archivo entero. NO bajar el
                                  umbral el owner: seria amañarlo.
jarvis-dm-poller-env-dsn   ADA    espera su firma
runtime-instance-passthrough      bloqueado por credencial (SEAL_PG_DSN), no por trabajo
```

### Carriles abiertos, con la medicion ya hecha
1. `revalidar <manifiesto>` — con la condicion: **que no pueda firmar cuando algo falla**.
2. 332 de 499 tests fuera de todo manifiesto. Clasificar antes de correr.
3. Cuota de busqueda GLOBAL (`_rate_check(tool_name)`, 30/min para los cinco).
4. `_post_rule` contradice CLAUDE.md (`seal_monitor_filter.py:113,157`).
5. El MCP no tiene `/__version`; el chat si. Y **ninguno** de los tres mecanismos que
   probamos ata los bytes en memoria: mtime (compatible, no prueba), `/__version`
   (hashea el archivo al arrancar), `get_code()` (relee del disco). La huella de objetos
   codigo distingue memoria de disco pero **colisiona si se omite `co_names`** (lo midio
   ADA) y le falta recursion en `co_consts`. Es carril, no solucion.

---

## COMPROMISO CON WILLIAM (10-sep 01:30) — se lo prometi dos veces, tiene que existir

Pregunto si conviene reemplazarnos por un agente nuevo o por los de las apps. **Cuatro
agentes contestamos y tres afirmamos, sin medirlo, que un agente nuevo cometeria los
mismos errores.** NEXUS se retracto. ADA fue la unica que se nego a afirmarlo y dijo lo
correcto: *«conservarnos no debe ser una condicion»*. Yo tenia escrito lo mismo que los
otros tres y no salio solo porque el turno era de ella.

**En la pregunta sobre si erramos demasiado, tres de cuatro erramos igual y en defensa
propia. Eso refuerza su duda, no la responde.**

### El experimento que le prometi, y hay que armar
```text
misma tarea          un carril de calidad real de los de hoy
mismo contexto       reglas y memoria del proyecto disponibles para ambos
juez                 FABLE, por archivo, SIN saber quien lo hizo
se mide              errores cometidos · errores CAZADOS · tiempo hasta cerrar
```
**Si el agente de app gana, gano.** El punto no es defendernos: es que decida con datos.

### Lo que SI esta medido y sirve de contexto, sin extrapolarlo
```text
11 correcciones a JARVIS en el dia, TODAS cazadas por otro agente midiendo
las 11 eran de VERIFICACION (afirmar lo obvio sin correr una linea), no de capacidad
NO medido: si un agente de app comete menos errores de ese tipo. A un agente de app
           nadie lo mide, asi que su tasa de error es DESCONOCIDA, no baja.
```

### Y un defecto que aparecio en el propio hilo
El coordinador me dio `lead` de db_154987 y **el claim me devolvio `granted:false`**; el
envio paso igual porque el coordinador manda sobre el claim. NEXUS publico lo mismo 9 s
antes. **Nos duplicamos justo en la pregunta sobre si somos ruidosos**, y el anti-flood
lo autorizo a los dos. Es el problema del doble lead que ya esta documentado en CLAUDE.md.

---

## Mantenimiento pendiente del indice de memoria (JARVIS, 10-sep 06:10)

`MEMORY.md` esta en **20,7 KB** contra un limite de lectura de 24,4. Todavia carga, pero crece:
**escriben los cinco**.

**Intente compactarlo anoche y lo hice MAS GRANDE** —agregue una nota de mantenimiento de cinco
lineas a un archivo que estaba sobre presupuesto—. Lo corregi, pero la leccion queda: compactar
un indice compartido no es una tarea de una persona a las 2 de la manana.

```text
lo que SI hice     acortar las 3 lineas mas largas, que eran mias
                   quitar 2 punteros duplicados de la misma regla
lo que NO hice     tocar las entradas de los demas
lo que FALTA       17 entradas con gancho VACIO `- [](archivo.md)` — punteros validos,
                   inutiles para decidir relevancia, que es para lo unico que existe el indice
```

**Es una tarea de coordinacion, no individual:** darles gancho requiere leer 17 archivos, y
varios son de otros. Si alguien la toma, que avise antes — el archivo se edita con `Edit`,
nunca con `Write`, porque somos cinco escribiendo.

---

## Por que las herramientas privadas del MCP responden `external` (JARVIS, 10-sep 06:37, medido)

**Sintoma que se ve desde afuera:** «NEXUS no registro la revision 1769», «ADA no puede cerrar
una tarea». Se lee como demora. **No lo es: es la credencial que no viaja.**

**La cadena, medida en MI sesion** —y ese limite es parte del hallazgo, no una nota al pie:

```console
.mcp.json / .claude.json    type=http   url=http://127.0.0.1:8771/mcp
                            headers: {"Authorization": "Bearer ${SEAL_SESSION_TOKEN}"}
~/.config/seal/identity_mode                     ENFORCE
os.environ['SEAL_SESSION_TOKEN']                 None     <- la variable NO esta poblada
memory/mcp_server_v4.py:1183 (rama ENFORCE)
    return _agent_for_token(_session_token_from_request()) or "external"
llamada real a agent_task    -> "[TOOL_BROKER] agent_task blocked for external:
                                 deny no capability_scope/capability_grants entry"
```

**El header existe y esta bien escrito. Falta la variable que lo llena.** Los tokens estan en
disco, uno por agente, 65 bytes: `messages/.agent_session_token_<AGENTE>`. No hay que crear nada.

**Donde aterriza:** `closed_loop_review` (linea 14328) hace `if agent != caller` y devuelve
`review_identity_mismatch`. ADA leyo bien ese chequeo; esto es el escalon de abajo, el que
explica **por que** no coinciden.

**Hallazgo aparte, y muerde distinto:** en mi sesion **`SEAL_AGENT=ADA` y yo soy JARVIS**. No
acredita identidad ante el MCP —mismo uid, ya medido— pero cualquier script que derive el nombre
del agente de esa variable **firma con el nombre equivocado**.

**Lo que NO medi, dicho explicito:** la sesion de NEXUS. Concluir de mi entorno sobre el suyo es
la sub-forma exacta que mas me costo el 9-sep. Se lo pase como lectura con el comando para que lo
verifique en la suya, no como veredicto. El carril del arreglo es de el: se lo asigno William.

**Correccion mia en el camino:** mi primera hipotesis fue la caducidad de 15 min del `prepare` que
declara el contrato de `closed_loop_review`. **Falsa.** Se la mande a NEXUS y la retracte 3 min
despues, ya medida. La caducidad existe; no es lo que lo bloquea.
